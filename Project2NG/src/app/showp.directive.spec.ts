import { ShowpDirective } from './showp.directive';

describe('ShowpDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowpDirective();
    expect(directive).toBeTruthy();
  });
});
